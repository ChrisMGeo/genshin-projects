import path from "path";
export const PROJECT_ROOT = path.resolve("..", "..", "packages", "gi-data"); // TODO: Find better way to get project root
export const GI_DM_ROOT = path.join(PROJECT_ROOT, "AnimeGameData");
